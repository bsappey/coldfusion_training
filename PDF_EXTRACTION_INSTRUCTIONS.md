# PDF Extraction Instructions

## Problem
The automated PDF text extraction isn't working well because PDFs are binary files. We need the actual function names and syntax from the 5 PDFs.

## Solution Options

### Option 1: Export PDFs to Text (Recommended)
1. Open each PDF in **Adobe Acrobat Reader** (or Adobe Acrobat Pro if you have it)
2. Go to **File > Export To > Text (Plain)**
3. Save each text file with the same name (e.g., `AI Models.txt`)
4. Place them in: `C:\Users\admin\Downloads\CF 2025.1 AI Alpha Docs\`
5. I can then parse the text files to extract function names and syntax

### Option 2: Copy/Paste Key Sections
1. Open each PDF
2. Find the "Function Reference" sections
3. Copy/paste the function names, syntax, and examples
4. Share them here and I'll organize them

### Option 3: Use Online PDF to Text Converter
1. Use an online tool like: https://www.ilovepdf.com/pdf_to_txt
2. Convert each PDF to text
3. Save the text files
4. I can parse them

## What We Need from Each PDF

### From AI Models.pdf:
- All function names (e.g., `getChatModel`, `getAIService`)
- Function syntax (tag and script versions)
- Parameters for each function
- Return types
- Code examples

### From Vector Store Function Reference.pdf:
- All vector store function names
- Function syntax
- Parameters
- Return types
- Code examples

### From Vector Stores.pdf:
- Concepts and architecture
- Use cases
- Configuration options

### From MCP Function Reference.pdf:
- All MCP function names
- Function syntax
- Parameters
- Return types
- Code examples

### From MCPs with ColdFusion.pdf:
- MCP concepts
- Setup instructions
- Configuration
- Examples

## Once We Have the Text Files

I can:
1. Parse the text files to extract function names
2. Extract syntax (tag and script)
3. Extract parameters and return types
4. Extract code examples
5. Update Module 13 with actual functions
6. Create code sample files
7. Update function reference

## Quick Start

**Easiest method**: Export PDFs to text using Adobe Acrobat Reader:
1. Right-click PDF → Open with → Adobe Acrobat Reader
2. File → Export To → Text (Plain)
3. Save as `.txt` file
4. Repeat for all 5 PDFs
5. Let me know when done and I'll parse them!
